services:
  postgres:
    image: postgres:17-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: dropworker
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?请设置 POSTGRES_PASSWORD}
      POSTGRES_DB: dropworker
    volumes: [postgres-data:/var/lib/postgresql/data]
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U dropworker -d dropworker"]
      interval: 5s
      timeout: 5s
      retries: 20

  minio:
    image: minio/minio:latest
    command: server /data --console-address :9001
    restart: unless-stopped
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER:?请设置 MINIO_ROOT_USER}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD:?请设置 MINIO_ROOT_PASSWORD}
    volumes: [minio-data:/data]
    ports: ["9001:9001"]

  create-bucket:
    image: minio/mc:latest
    depends_on: [minio]
    entrypoint: ["/bin/sh", "-c"]
    command:
      - >-
        until mc alias set local http://minio:9000 $$MINIO_ROOT_USER $$MINIO_ROOT_PASSWORD; do sleep 2; done;
        mc mb --ignore-existing local/drop-worker
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER:?请设置 MINIO_ROOT_USER}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD:?请设置 MINIO_ROOT_PASSWORD}

  migrate:
    build: ../..
    command: npm run admin -- migrate-database
    depends_on:
      postgres: { condition: service_healthy }
    environment: &app-env
      DATABASE_DRIVER: postgres
      DATABASE_URL: postgresql://dropworker:${POSTGRES_PASSWORD}@postgres:5432/dropworker
      DATABASE_ALLOW_INSECURE: "true"
      BLOB_DRIVER: s3
      S3_ENDPOINT: http://minio:9000
      S3_ALLOW_INSECURE: "true"
      S3_REGION: us-east-1
      S3_BUCKET: drop-worker
      S3_PREFIX: drop-worker/
      S3_FORCE_PATH_STYLE: "true"
      S3_ACCESS_KEY_ID: ${MINIO_ROOT_USER}
      S3_SECRET_ACCESS_KEY: ${MINIO_ROOT_PASSWORD}

  drop-worker:
    build: ../..
    restart: unless-stopped
    depends_on:
      migrate: { condition: service_completed_successfully }
      create-bucket: { condition: service_completed_successfully }
    environment:
      <<: *app-env
      PORT: 3000
      PUBLIC_URL: ${PUBLIC_URL:-http://localhost:3000}
      AUTH_MODE: password
      ADMIN_EMAIL: ${ADMIN_EMAIL:?请设置 ADMIN_EMAIL}
      ADMIN_PASSWORD_HASH: ${ADMIN_PASSWORD_HASH:?请设置 ADMIN_PASSWORD_HASH}
      SESSION_SECRET: ${SESSION_SECRET:?请设置 SESSION_SECRET}
    ports: ["3000:3000"]

volumes:
  postgres-data:
  minio-data:
