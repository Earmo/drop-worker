# AGENTS.md

## 项目说明

- 本仓库当前处于初始化阶段；实现功能前先阅读现有代码、配置和文档。
- 优先遵循仓库已有的架构、命名、格式化和测试约定，不引入无关重构。
- 需求或技术栈不明确时，先通过现有文件确认；仍无法判断且会影响结果时，再向用户提问。

## 开发约定

- 修改应保持范围清晰、易于审查，并避免覆盖用户已有的未提交更改。
- 搜索文件和内容时优先使用 `rg`。
- 手工编辑文件时保持原有编码和换行风格；新增文本默认使用 UTF-8。
- 新增或修改行为时补充与风险相称的测试。
- 完成工作后运行相关格式化、静态检查和测试，并如实说明未能执行的验证。

## 工程目录管理规范

- 新增文件前必须先使用 `rg --files` 检查现有目录和同类文件，优先复用已有目录、命名和组织方式；不得因为“方便”在根目录或随意新建 `misc`、`common`、`tmp`、`new` 等无明确归属的目录。
- 根目录只放项目级入口文件、构建/包管理配置、许可证、README 和代理规范等确有必要的元数据。业务代码、功能模块、测试、脚本、文档和临时文件不得直接堆放在根目录。
- 生产代码放入仓库已有的源码目录（当前包括 `app`、`api`、`packages`、`server`、`worker` 等），并按现有模块边界归位；`api/` 按领域（`items`、`uploads`、`sharing`、`cleanup`、`auth`）拆分，功能专属代码优先与所属功能相邻，只有确实被多个模块复用且职责稳定时才提升到共享目录。
- 测试统一放入现有测试目录（当前为 `tests`），并沿用仓库已有的命名和分层方式；文档放入 `docs`，可执行脚本放入 `scripts` 或 `tools`，代理专属资料放入 `.agents` 或 `docs/agents`。不要为单个文件另建一层目录。
- 构建产物、缓存、覆盖率报告和本地实验文件必须放入已有的 `build`、`dist`、`.next`、`.scratch` 等约定目录，并确认已被 `.gitignore` 或明确的产物规则覆盖；禁止手工修改生成文件或把生成物当作源码提交。
- 新建目录必须能用一句话说明其职责，并且至少包含一组同类文件或明确的扩展计划；单个零散文件应放入最近的既有目录，而不是通过目录层级掩盖归属不清。
- 不得创建含义重复的平行目录（例如 `util` 与 `utils`、`test` 与 `tests`、多个同义功能目录）。若确需调整目录结构，应同步更新导入路径、构建配置、测试、CI 和文档，并在改动说明中写明迁移原因。
- 临时脚本、调试输出和一次性数据只允许放在 `.scratch` 或其他已约定的临时位置，任务完成后清理；不得留下带日期、序号或 `final-new` 等后缀的临时副本。
- 完成改动前检查 `git status --short` 和新增文件路径，确保每个文件都位于职责明确的目录、没有意外生成物或重复文件；目录归属不明确时先阅读相关配置和文档，再选择位置。

## 代码注释规范

- 代码注释必须足够说明实现意图和边界；代理在新增或修改逻辑时，应主动为所有非显而易见的代码补充注释，不能只在用户明确要求时才添加。
- 以下位置必须有准确的注释或文档注释：导出的模块、类、函数、类型和配置项；涉及业务规则、权限、数据一致性、状态转换、并发、缓存、重试、超时、资源清理、兼容性、第三方协议或安全约束的逻辑；以及任何需要读者结合上下文才能理解的条件分支、算法、正则、魔法数字和临时规避方案。
- 注释应优先解释“为什么这样做”、前置条件、关键不变量、取舍、失败模式和边界情况；对于公共 API，还要说明参数含义、返回值、异常或错误结果，以及调用方必须遵守的约束。复杂流程应在入口或关键转折处提供简短的步骤说明，必要时给出输入输出示例。
- 注释必须与当前代码、类型和实际运行行为一致。修改行为、接口、错误处理或约束时，必须同步更新受影响的注释；发现已有注释不准确、含糊或过时时，应在同一改动中修正，不能保留误导性说明。
- 不要为每一行机械复述语法，也不要添加无法验证的推测、空泛描述或把实现细节写死的冗长段落。能通过清晰命名表达的内容不必重复，但仅靠命名无法表达的动机、风险和约束必须写出来。
- 注释使用与代码一致的语言，优先使用简洁、完整的句子；避免“临时”“这里处理一下”等没有上下文的措辞。TODO 注释必须说明待解决的问题、原因和后续动作，若有 issue 或文档编号应一并引用。
- 提交前检查本次变更涉及的复杂逻辑是否都有注释，并确认注释回答了“为什么、边界是什么、失败时怎样”的问题；代码审查发现注释不足或不准确时，应将其视为需要修复的问题，而不是可选的润色。

## Git 约定

- 默认分支为 `main`。
- 未经用户明确要求，不创建提交、不推送远端，也不改写 Git 历史。
- 提交信息使用简体中文，主题简洁说明变更目的；必要时在正文补充原因和验证结果。
- 不提交密钥、令牌、私有配置、构建产物或依赖缓存。

## `.agents` 目录

- `.agents/` 用于存放项目专属的代理配置、角色说明或辅助资料。
- 新增代理配置时，应使用含义明确的文件名，并在文件中说明职责、输入、输出和边界。

## Agent skills

### Issue tracker

议题与规格使用仓库内的本地 Markdown 管理。详见 `docs/agents/issue-tracker.md`。

### Triage labels

使用默认五类分流标签。详见 `docs/agents/triage-labels.md`。

### Domain docs

采用单上下文领域文档布局。详见 `docs/agents/domain.md`。
